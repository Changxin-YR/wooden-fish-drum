# 变更记录

## 2026-07-31｜静心木鱼 V2.1

- 状态：`in_progress`
- 用户请求：以改良报告 V2 和新 UI 素材为基线，将现有工程增量升级为木鱼、念珠和一炷香三工具应用，并继续支持 phone、tablet、2in1。

### 当前进度

- 已提交 V2.1 权威设计规格和分阶段实施计划。
- 已复现改造前基线问题：标准检查含两个失效路径，设置页使用 API 24 不支持的 `Row.flexWrap`。
- 已将文字候选容器改为 API 24 支持的 `Flex`，并纠正标准检查中的失效资源路径。
- 标准检查与 debug 构建均已通过；debug unsigned HAP 为 4,574,533 bytes。
- 已增加 V2.1 场景、工具和练习状态枚举，以及版本化设置字段和幂等旧设置迁移。
- 设置迁移测试完成 RED/编译失败验证；ohosTest HAP 与主 debug HAP 均重新构建通过。
- 当前无可用 HDC 设备，新增 Hypium 用例的运行态执行留待设备恢复后完成。
- 已新增 `DailyStats`、`PracticeSession`、旧统计迁移和练习历史 Repository。
- 会话历史支持 ID 去重、倒序读取、按日期/全部清除；每日统计支持会话幂等汇总。
- 保留旧 `DailyRecord` 展示接口，现有首页和统计页在 UI 重建前继续兼容运行。
- Task 3 的标准检查、ohosTest 构建和主 debug 构建均通过。
- 已新增单定时器 `AutoRhythmController`、墙钟驱动 `IncenseTimer` 和统一 `PracticeController`。
- 自动节律支持单实例、暂停、继续和后台清理；香计时支持暂停校正和完成事件去重。
- 新控制器已注册到运行时；旧 `SessionService` 仅作为当前 V1 首页的可构建过渡层保留。
- Task 4 的标准检查、ohosTest 构建和主 debug 构建均通过。
- 已按批准值固化程序员、静心、日常三场景默认配置。
- 已新增两步首次引导：先选场景，再选木鱼/念珠/一炷香默认工具。
- `Index` 根据 `initialized` 显示引导或主壳；升级用户不会被强制重播引导。
- Task 5 的标准检查、ohosTest 构建和主 debug 构建均通过。
- 已将用户提供的木鱼、念珠、一炷香透明 PNG 按 alpha 主体边界无损裁切并入库。
- 已记录三张 phone 页面参考稿、裁切坐标、授权状态和发布限制。
- 当前缺少无光圈木鱼、香体/烟雾分层源图和商用授权证明，发布资源验收保持 `blocked`。
- Task 6 的标准检查和主 debug 构建通过，HAP 为 7,044,893 bytes。
- 已新增强类型 `PracticeEvent`；连续 100 次手动输入不丢计数，手动与自动命中共享递增反馈序列。
- 活动练习页已从旧 `SessionService/TARGET` 流切换到 `PracticeController`，音效或振动作为计数后的独立反馈执行。
- 已新增今日概览和工具控制台，按标题、离线标识、今日数据、三工具、练习画布、计数、控制台的 V2.1 层级重建主页。
- 木鱼页优先使用用户提供的 `practice_wooden_fish.png`，点击热区覆盖整个画布；波纹、回弹、浮字和减少动画由 ArkUI 控制。
- phone 使用单栏，tablet/2in1 在 600vp 起将练习画布与控制台重排为双栏；工具选中态同时显示边框和勾选。
- Task 7 标准检查、ohosTest HAP 和主 debug HAP 构建通过，主 HAP 为 7,115,910 bytes。
- 当前无 HDC 目标；DevEco `PreviewBuild` 以内部错误 `00308018` 失败，phone 运行截图对照暂时 `blocked`。
- 已新增 `BeadsView`，按 604×1095 源图坐标系将 27 个珠位等比映射到练习画布。
- 念珠点击和轻扫统一为带 ID 的手势事件；控制器拒绝同一手势的重复更新，当前珠位同时使用增亮层和外圈反馈。
- 27、54、108 节点只在对应次数发出一次强类型节点事件，109 次不会重复 108 提示；节点振动遵循用户开关。
- 实时统计新增按工具增量入口，念珠手动/自动次数不再误记到木鱼分类。
- 切出念珠时立即停止自动节律并发出会话草稿；运行时将草稿追加到真实本地练习历史，避免记录页伪造数据。
- Task 8 RED/GREEN 的 ohosTest 构建和主 debug 构建均已通过，HAP 分别为 8,112,970 和 7,160,463 bytes；运行态 Hypium 仍等待 HDC 设备。
- 修正构建脚本跨目标误选“最新 HAP”的问题，产物验证现限定到本次 `$Target` 输出目录。
- 已新增 `IncenseView` 和 5/10/15/20/30 分钟预设，展示限制在 0..1 的燃烧进度、剩余时间、暂停/继续/结束状态。
- 香计时接入统一 `PracticeController`，使用墙钟校正暂停和前后台时间；重复完成刷新只提交一条会话。
- 活动香状态以独立快照持久化开始时间、目标时长、暂停点、累计暂停和状态；新控制器实例可恢复准确剩余时间。
- 活动快照与会话历史共用串行写入队列，完成或快速暂停时不会让旧快照覆盖清除操作。
- 正常完成和提前结束均保存真实时长与 `completed`，并将香时长幂等汇总到每日统计。
- 当前香素材的烟雾和主体未分层，因此保持原图静态烟雾；减少动画模式不会增加位移，发布前分层资源仍为 `blocked`。
- Task 9 ohosTest HAP 与主 debug HAP 构建通过，分别为 8,140,113 和 7,228,343 bytes；运行态验证等待 HDC 设备。
- 下一步按 V2 记录稿重建记录页，接入真实练习历史和次数/时长分离统计。
- phone 模拟器恢复后发现练习页沿用 330vp 宽屏画布，导致控制台需要滚动才能到达；已通过
  `PracticeLayout` 强类型预算将 phone 画布调整为 150vp，并按 V2 稿把提示语移到节律按钮之后。
- 木鱼、念珠和一炷香三工具现均可在 1320×2856 phone 模拟器单屏完整展示；音量滑杆保留至少
  64vp，所有命令触控目标保持至少 48vp，最终截图归档在 `docs/qa/screenshots/muyu-v2-*-fit.jpeg`。
- 新增 phone 布局回归后，设备 Hypium 为 41/41，Failure 0、Error 0；debug 与 ohosTest HAP
  重新构建通过。正式发布仍受素材授权、无光圈木鱼、香体/烟雾分层和签名身份阻塞。
- 记录页已改为今日、本周、本月、全部四个真实周期；次数仅包含木鱼和念珠，练习时长与
  一炷香时长分别汇总和绘图，不跨单位计算同一比例。
- 旧每日汇总只进入统计和趋势，不生成虚假会话；最近练习只读取本地 `PracticeSession`，
  按 `startTime` 倒序，空状态不包含设计稿中的示例时间。
- Task 10 设备 Hypium 为 45/45，Failure 0、Error 0；主 debug 与 ohosTest HAP 构建通过。
  API 24 phone 模拟器已验证今日/全部切换、48vp 周期触控区和空会话首屏。
- 设置页已按练习、音频、显示、文字与触感、数据管理、关于与合规六组重建；未加入 V2.1
  未承诺的背景音乐或数据导出入口。
- 清除今日和清除全部已完成 RED/GREEN 回归；清除今日仅删除当天统计与会话，清除全部删除
  设置、统计、历史和活动香快照并返回首次引导。两类操作均在 phone 模拟器验证二次确认并取消，
  未改动验收数据。
- 已新增隐私说明、用户须知、开源与许可、关于应用四个真实二级页面；phone 模拟器验证页面
  内容、返回按钮、正文滚动和二级页面隐藏底部主导航。
- 外部 HarmonyOS 标准检查器 22 项通过、0 项失败、1 项提示；提示为未设置 `common/` 目录，
  本项目继续使用职责明确的 `models/`、`constants/`、`utils/`，不为目录形式搬迁可用代码。
- Task 11 最终门禁：项目标准检查通过；Hypium 47/47，Failure 0、Error 0；debug、ohosTest、
  release 构建通过。debug HAP 为 7,396,792 bytes，ohosTest HAP 为 8,185,899 bytes，release
  HAP 为 6,938,600 bytes。
- 上架代码侧审计确认 `module.json5` 声明 phone、tablet、2in1，主模块只申请
  `ohos.permission.VIBRATE`，未发现网络权限、HTTP、WebView、广告、支付、登录或云同步能力。
- release 仍为 unsigned HAP；真实发布签名、备案信息、AppGallery 隐私政策公网 URL、素材商业
  授权以及 tablet/2in1 运行态证据保持发布阻塞，详见 `docs/qa/2026-07-31-release-readiness.md`。

## 2026-07-30｜用户反馈改版

- 状态：`done`
- 用户请求：逐项修复测试反馈，并吸收同类产品的大触控区、可见音色和自动敲击优点，同时彻底排除广告与联网冗余。

### 已完成改动

- 统一为普通用户，移除首启三用户选择和设置页用户切换，兼容迁移旧偏好。
- 首页顶部仅保留今日功德；浮字不再追加次数，并在 760ms 后自动消退。
- 删除限时档；自由、目标、自动三档互斥，自动档支持再次点击关闭，切档立即停止。
- 目标档增加 108 次进度、完成反馈和额外敲击拦截。
- 实现日、周、月、年四套真实统计口径、标题、摘要和图表桶。
- 实现跟随系统、浅色、深色完整主题，以及低沉、清脆、柔和三音色可见选择和本地试听。
- 将木鱼主体与 ArkUI 木槌拆分；手动和自动共用同一敲击序列与动画反馈。
- 保留用户原素材，新增仅移除静态木槌的浅色/深色木鱼主体派生资源。
- 记录小米 15 上同类产品只读观察结果；不复制其品牌素材，也不引入广告、登录、排行或外部跳转。

### 验证

- 项目标准检查与外部 HarmonyOS 标准检查：`passed`。
- debug 主 HAP：`passed`，5,166,513 bytes。
- release 主 HAP：`passed`，4,986,221 bytes。
- ohosTest HAP：`passed`，6,218,038 bytes。
- Hypium：`passed`，14/14，Failure 0，Error 0。
- 代码与权限扫描：`passed`，主模块仅申请振动权限，未发现网络权限、HTTP、WebView 或广告 SDK。
- phone 模拟器：主题、音色面板、统计周期标题与自动切档停止已验证；目标 108 封顶由 Hypium 回归用例验证。
- tablet、2in1：`blocked`，当前没有对应设备。

### 遗留风险

- 未配置签名身份，产物保持 unsigned HAP。
- 模拟器无法客观证明实体扬声器音色与真实振感。
- 模拟器中另一应用偶发抢占自动化输入焦点，因此未将目标完成坐标点击作为运行态证据；封顶与额外敲击拦截由可复现的 Hypium 用例覆盖。

## 2026-07-30｜功德木鱼 V1.0

- 状态：`done`
- 用户请求：按批准的高保真设计快速准确完成完整 HarmonyOS V1.0。
- 项目类型：API 24 Stage 模型 ArkTS/ArkUI 单机应用。

### 已完成改动

- 建立 API 24 Stage 工程、本地 Hypium 依赖、静态门禁与 debug/release 构建脚本。
- 实现三种用户模式、首次启动选择、模式切换与设置持久化。
- 实现手动、目标、限时、自动会话状态机及后台停止。
- 实现每日统计、最近 7 日补零、累计/手动/自动/连续/最大单日派生与二次确认清除。
- 实现木鱼回弹、SoundPool 三音色、音量、振动与最多三条浮动文字。
- 实现模式选择、首页、统计、设置和底部导航，接入用户原始高保真素材。
- 从用户图标板原样裁切应用图标并配置到 AppScope 与 EntryAbility。
- 增加 Compact/Medium/Expanded 断点和宽屏限宽布局。
- 修复快速连续敲击时 Preferences 写入乱序导致的计数丢失，并增加乱序写入回归用例。
- 修复 ArkUI Builder 中汇总值未实时刷新的状态观察问题。
- 从用户 UI 图标板原样裁切首页、统计、设置图标，替换可能被系统渲染为彩色 Emoji 的字符。

### 验证

- 项目标准检查：`passed`。
- 外部 HarmonyOS 标准检查：`passed`，仅有可选 `common/` 目录提示。
- 禁止能力扫描：`passed`，未发现网络、WebView 或 ArkTS `any`。
- debug 主 HAP：`passed`，1,667,250 bytes。
- release 主 HAP：`passed`，1,493,515 bytes。
- ohosTest HAP：`passed`，2,588,515 bytes。
- Hypium 运行态：`passed`，10/10，Failure 0，Error 0。
- phone 设备与交互：`passed`，目标 `127.0.0.1:5555`，已归档 8 张运行截图。
- tablet、2in1 设备交互：`blocked`，当前没有对应目标；响应式代码与构建门禁已通过。
- 证据索引：`docs/qa/2026-07-30-build.md`、`docs/qa/2026-07-30-interaction.md`、`docs/ui/asset-manifest.md`。

### 遗留风险

- 未配置签名身份，当前产物为 unsigned HAP；保持了用户要求的不擅自修改签名身份。
- 音频听感和真实振感无法由模拟器截图客观证明；服务调用、开关和降级路径已通过构建与代码门禁。
- tablet、2in1 的实际运行截图仍需对应设备，不以 phone 结果替代。
