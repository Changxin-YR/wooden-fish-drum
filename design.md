# 产品与技术设计

## 产品目标

构建东方极简、温暖、安静的 HarmonyOS 单机功德木鱼应用。完整设计见 `docs/superpowers/specs/2026-07-29-muyu-v1-design.md`。

## 页面信息架构

| 页面 | 职责 | 主要状态 |
| --- | --- | --- |
| ModeSelectPage | 首次选择用户模式 | 默认、选中、保存失败 |
| HomePage | 木鱼与会话控制 | 自由、目标、限时、自动 |
| StatsPage | 本地统计与趋势 | 空数据、正常数据 |
| SettingsPage | 模式、反馈、外观和数据 | 默认、编辑、二次确认 |

## 视觉系统

- 浅色背景 `#F6F1E8`，深色背景 `#171513`，点缀色 `#A9824B`。
- 手机按批准高保真图还原；tablet 与 2in1 只重排，不拉伸素材。
- Compact `<600vp`、Medium `600-1023vp`、Expanded `>=1024vp`。

## 状态与数据

- Preferences 保存设置和每日统计 JSON。
- 三种用户模式共用页面和服务，只读取统一配置。
- 会话状态不跨应用重启；进入后台停止定时器并刷新数据。

