# 产品与技术设计

## 产品目标

构建东方极简、温暖、安静的 HarmonyOS 单机功德木鱼应用。完整设计见 `docs/superpowers/specs/2026-07-29-muyu-v1-design.md`。

## 页面信息架构

| 页面 | 职责 | 主要状态 |
| --- | --- | --- |
| HomePage | 木鱼与会话控制 | 自由、目标、自动、目标完成 |
| StatsPage | 本地统计与趋势 | 日、周、月、年、空数据 |
| SettingsPage | 反馈、音色、外观和数据 | 默认、音色面板、二次确认 |

## 视觉系统

- 浅色背景 `#F6F1E8`，深色背景 `#171513`，点缀色 `#A9824B`。
- 手机按批准高保真图还原；tablet 与 2in1 只重排，不拉伸素材。
- Compact `<600vp`、Medium `600-1023vp`、Expanded `>=1024vp`。

## 状态与数据

- Preferences 保存设置和每日统计 JSON。
- 运行时统一为普通用户；旧程序员/僧侣快照在解码时迁移，同时保留用户偏好。
- `SessionService` 统一管理自由、目标、自动三档互斥状态；自动档同档点击关闭，切档立即清理定时器。
- 每次手动或自动敲击递增同一个 `hitSequence`，页面据此驱动木槌、木鱼、波纹与浮字。
- `StatsPeriodService` 从本地每日记录生成日、周、月、年四套桶和摘要。
- `ThemeResolver` 将系统色彩模式与用户的跟随/浅色/深色选择解析为最终主题。
- 会话状态不跨应用重启；进入后台停止定时器并刷新数据。

## 源码职责

- `pages/` 负责编排，`components/` 负责复用 UI，`repositories/` 负责持久化，`services/` 负责系统能力。
- 通用职责由 `models/`、`constants/`、`utils/` 分离承载；不额外建立 `common/`，避免为目录形式迁移稳定代码。

## 隐私与商业边界

- 不申请网络权限，不接入广告 SDK、WebView、登录、排行、支付或云同步。
- 所有敲击记录、偏好和统计仅保存在本机 Preferences。

