# 2026-07-30 构建验证

## 环境

- HarmonyOS SDK：6.1.1 API 24
- Hvigor：6.24.3
- 包名：`com.max.muyu`
- 设备：`hdc list targets` 返回 `[Empty]`

## 结果

| 门禁 | 结果 | 产物/说明 |
| --- | --- | --- |
| `scripts/check-standard.ps1` | passed | 必要工程、页面、服务、素材和测试文件齐全 |
| 外部项目标准检查 | passed | 0 failures；仅提示可选 `common/` 目录 |
| 禁止能力扫描 | passed | 无网络、WebView、HTTP 或 ArkTS `any` |
| debug 主 HAP | passed | `entry/build/default/outputs/default/entry-default-unsigned.hap`，1,541,941 bytes |
| release 主 HAP | passed | 同一输出名，release 构建时 1,370,493 bytes |
| debug ohosTest HAP | passed | `entry/build/default/outputs/ohosTest/entry-ohosTest-unsigned.hap`，2,582,036 bytes |
| Hypium 运行 | blocked | 无设备，无法安装测试 HAP 并运行 `OpenHarmonyTestRunner` |

项目未配置签名身份，Hvigor 仅报告 `No signingConfig found`，产物保持 unsigned。
