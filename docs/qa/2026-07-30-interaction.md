# 2026-07-30 交互验收清单

当前无连接设备，以下运行态检查均为 `blocked`，不是 `passed`。连接对应 API 24 设备后按顺序恢复：

1. 首次启动进入模式选择，默认选中程序员；确认后进入首页，返回键不能回到选择页。
2. 强制停止并重启，直接进入首页且模式、主题、音色、开关和自定义文字保持。
3. 快速手动敲击 100 次，今日和累计精确增加 100；每次有一次回弹和一次反馈计划，浮动文字最多三条。
4. 验证低沉、清脆、柔和三音色、0–100% 音量、振动开关和静默降级。
5. 目标模式在第 108 次停止；限时模式倒计时结束；自动模式重复开始不创建第二定时器。
6. 自动或限时运行时把应用切到后台，计时器立即停止。
7. 切换用户模式，历史统计、自定义文字和手动主题不丢失。
8. 统计页核对今日、累计、手动、自动、连续天数、最大单日和最近 7 日补零。
9. 清除统计第一次取消不变；第二次确认后统计归零、设置和模式保留。
10. 分别在 phone、tablet、2in1 验证安全区、底部导航、宽屏限宽、长中文和大字体无重叠。
